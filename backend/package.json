{
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.5",
    "sequelize-cli": "^6.6.2"
  },
  "scripts": {
    "setup:dev": "npx sequelize-cli db:create && npx sequelize-cli db:migrate",
    "setup:test": "NODE_ENV=test npx sequelize-cli db:create && npx sequelize-cli db:migrate",
    "setup:prod": "NODE_ENV=production npx sequelize-cli db:create && npx sequelize-cli db:migrate"
  }
}
